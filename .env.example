# Supabase Configuration
EXPO_PUBLIC_SUPABASE_URL=your_supabase_url_here
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace the values with your actual Supabase credentials
# 3. .env.local is already in .gitignore and will not be committed

# ============================================
# IMPORTANT: Edge Function Secrets
# ============================================
# The following API keys must be set as Supabase Edge Function secrets
# (NOT in this .env file - they are server-side only)
#
# To set Edge Function secrets:
# 1. Go to Supabase Dashboard → Project Settings → Edge Functions
# 2. Add the following secrets:
#    - OPENAI_API_KEY (for Whisper transcription)
#    - ANTHROPIC_API_KEY (for Claude insight generation)
#
# Without these, the transcription and insight generation will fail.
# ============================================
